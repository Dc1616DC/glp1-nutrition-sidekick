'use client';

interface MealContextWarningsProps {
  mealType?: string;
}

// Injection tracking removed - this component is disabled
export default function MealContextWarnings({ mealType }: MealContextWarningsProps) {
  return null;
}

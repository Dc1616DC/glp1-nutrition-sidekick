# -------------------------------------
# Firebase Client-Side Configuration
# -------------------------------------
# These keys are safe to expose on the client-side (the browser).
# Find them in your Firebase project settings under:
# Project settings > General > Your apps > (Select your web app) > Firebase SDK snippet > Config
#
# Instructions:
# 1. Create a new file in the root directory named ".env.local"
# 2. Copy the content of this file into ".env.local"
# 3. Replace the placeholder values below with your actual Firebase project credentials.
#
# The "NEXT_PUBLIC_" prefix is required by Next.js to expose these variables to the browser.

NEXT_PUBLIC_FIREBASE_API_KEY="YOUR_API_KEY"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="YOUR_PROJECT_ID.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="YOUR_PROJECT_ID"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="YOUR_PROJECT_ID.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="YOUR_SENDER_ID"
NEXT_PUBLIC_FIREBASE_APP_ID="YOUR_APP_ID"


# -------------------------------------
# OpenAI API Key (Server-Side ONLY)
# -------------------------------------
# This key is a secret and should ONLY be used on the server-side (e.g., in API routes).
# NEVER expose this key to the client-side.
# Find this key in your OpenAI account dashboard.

OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# -------------------------------------
# Firebase Admin SDK (Server-Side ONLY)
# -------------------------------------
# These are required for server-side authentication verification.
# NEVER expose these keys to the client-side.
# 
# Option 1: Individual environment variables (recommended for production/Vercel)
FIREBASE_PROJECT_ID="YOUR_PROJECT_ID"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@YOUR_PROJECT_ID.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----"

# Option 2: Full service account JSON (easier for local development)
# FIREBASE_ADMIN_SDK='{"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}'

# -------------------------------------
# Additional API Keys (Server-Side ONLY)
# -------------------------------------
SPOONACULAR_API_KEY="YOUR_SPOONACULAR_API_KEY"
GROK_API_KEY="YOUR_GROK_API_KEY"

# -------------------------------------
# Development Settings (Optional)
# -------------------------------------
# Enable development authentication fallback (NEVER use in production)
# ALLOW_DEV_AUTH="true"
# DEV_USER_IDS="test-user-1,test-user-2"
